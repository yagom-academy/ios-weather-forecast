//
//  WeatherForecast - ViewController.swift
//  Created by yagom. 
//  Copyright © yagom. All rights reserved.
// 

import UIKit

class ViewController: UIViewController {
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
//        let apiResource = APIResource(method: .get, weatherURL: .weatherCoordinates(latitude: 37.493370, longitude: 127.919701))
//        APIManager.requestAPI(CurrentWeather.self, WeatherError.self, resource: apiResource, decodeManager: DecodingManager()) { result in
//            dump(result)
//        }
        
        //        let apiResource = APIResource(method: .get, weatherURL: .forecastCoordinates(latitude: 37.493370, longitude: 127.919701))
        //        APIManager.requestAPI(FiveDaysWeather.self, WeatherError.self, resource: apiResource, decodeManager: DecodingManager()) { result in
        //            dump(result)
        //        }
    }
}

